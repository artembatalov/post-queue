services:
  postqueue:
    image: postgres:16
    environment:
      POSTGRES_USER: postqueue
      POSTGRES_PASSWORD: postqueue
      POSTGRES_DB: postqueue
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
